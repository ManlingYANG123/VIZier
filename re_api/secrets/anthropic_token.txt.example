# Put your Salesforce Express LLM Gateway API token on the first non-comment line
# of a sibling file named `anthropic_token.txt` (this .example is NOT read).
#
# That file is git-ignored. Alternatively export ANTHROPIC_AUTH_TOKEN in the env.
# See ../../../twbx2vegalite/use_api.txt for how to obtain a key from DevBar.
sk-your-token-here
